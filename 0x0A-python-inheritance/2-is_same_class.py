#!/usr/bin/python3
"""
check if the object is an instance or a class
"""


def is_same_class(obj, a_class):
    """
    check if the object is an instance or a class
    """
    if (type(obj) == a_class):
        return True
    return False
